
!keys.json.example
# Android (Capacitor) — generado por `npx cap add android` / build-web
# Archivos sensibles (no subir claves jamás)
# Audio de prueba generado por piper_compare.py
# CV generado por generar_cv.py desde LINKEDIN.md — versionado por petición
# Entorno de desarrollo local (no subir)
# Informe generado por python test_mutacion.py --coverage (regenerable)
# Node
# Piper TTS local (venv + modelos de voz, ~600 MB — no subir)
# Python
# Scripts temporales de validación/pruebas
# Sistema
# Token persistente del puente (lo genera windows/aion-sincro.cmd en modo repo)
# Venv de herramientas OSINT (holehe para aion_osint.py)
# `python generar_cv.py`, aparecerá como modificado: es correcto.
# expresa (los reclutadores pueden verlo en el repo). Al regenerar con
*.env
*.keys
*.pyc
*.tmp_*
.DS_Store
.env.*
.freebuff/
.tmp_*
.venv-osint/
.venv-piper/
MUTACION_COBERTURA.md
Thumbs.db
__pycache__/
desktop-v2.db
desktop-v2.db-shm
desktop-v2.db-wal
keys.json
mobile/.gradle/
mobile/android/
mobile/www/
node_modules/
piper-voices/
privado/
proxy_keys.json
salida-voces/
token
